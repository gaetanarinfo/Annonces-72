security:
    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    providers:
        from_database:
            entity:
                class: App\Entity\User
                property: username

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            anonymous: true
            user_checker: App\Security\UserChecker
            access_denied_url: /
            logout:
                path: /logout
                target: /    
            remember_me:
                secret:   '%kernel.secret%'
                lifetime: 604800 # 1 week in seconds
                token_provider: 'Symfony\Bridge\Doctrine\Security\RememberMe\DoctrineTokenProvider'
                path:     /
                # by default, the feature is enabled by checking a
                # checkbox in the login form (see below), uncomment the
                # following line to always enable it.
                #always_remember_me: true    

            #http_basic: true

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#firewalls-authentication

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

            form_login: 
                check_path: login         

    role_hierarchy:
        ROLE_PRO: ROLE_ADMIN
        ROLE_ADMIN: ROLE_ADMIN
        ROLE_SUPER_ADMIN: ROLE_ADMIN, ROLE_PRO

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        - { path: ^/admin/utilisateur, roles: ROLE_SUPER_ADMIN }
        - { path: ^/admin/website/, roles: ROLE_SUPER_ADMIN }
        - { path: ^/admin/biens, roles: ROLE_PRO }
        - { path: ^/admin/louer, roles: ROLE_PRO }
        - { path: ^/admin/appartement-a, roles: ROLE_PRO }
        - { path: ^/admin/appartement-b, roles: ROLE_PRO }
        - { path: ^/admin/option, roles: ROLE_PRO }
        - { path: ^/admin/option2, roles: ROLE_PRO }
        - { path: ^/admin/option3, roles: ROLE_PRO }
        - { path: ^/admin/option4, roles: ROLE_PRO }
        - { path: ^/admin/blog, roles: ROLE_PRO }
        - { path: ^/admin/mailbox, roles: ROLE_PRO }
        - { path: ^/user, roles: ROLE_USER }

    encoders:
        App\Entity\User:
            algorithm: bcrypt
            cost: 12
